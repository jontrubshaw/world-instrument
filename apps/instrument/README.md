# Instrument App

Browser shell for World Instrument visual scenes.

## Local development

```bash
npm install
npm run dev
```

The Vite dev server starts the app at the local URL printed in the terminal.

## Verification

```bash
npm run build
npm run test:smoke
```

The smoke test starts the Vite dev server and verifies that the browser app
loads.
